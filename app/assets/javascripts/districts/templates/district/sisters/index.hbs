<div class="text-center">
    <table class="table table-hover">
      <tbody>
        {{#each sister in sortedSisters}}
          <tr>
            <td>{{sister.fullName}}</td>
            <td><button {{action 'showModal' 'sister-modal' sister}}>Edit</button></td>
          </tr>
        {{/each}}
      </tbody>
  </table>
</div>
